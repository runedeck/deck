# Tasks

## 1. Implementation

- [x] 1.1 `Install the pinned rune` step in `quality.yaml`, `RUNE_CLI_REV` set to cli main `64fc7e85fbb9`
- [x] 1.2 Job timeout 30 minutes

## 2. Verification

- [x] 2.1 actionlint, zizmor, and both prek stages locally
- [ ] 2.2 The `quality` check on this change's pull request runs the `ontology-graph` hook and passes

## 3. Record

- [ ] 3.1 Archive moves `adr.md` to `docs/decisions/` with the next free DECK number
