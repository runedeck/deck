# Tasks

## 1. Rewrite

- [x] 1.1 Purposes for `adoption-session-state` and `portable-skill-authoring`, H1 fixes for three specs
- [x] 1.2 Fold the duplicate identity requirement, move the gate requirement into `spec-presence-attestation`

## 2. Verification

- [x] 2.1 `rune spec validate` and `rune spec doctor` print no error
- [x] 2.2 Both prek stages in an isolated clone

## 3. Record

- [ ] 3.1 Archive moves `adr.md` to `docs/decisions/` with the next free DECK number
- [ ] 3.2 `rune spec archive` writes a purpose from the proposal's Why instead of `TBD` (cli follow-up)
