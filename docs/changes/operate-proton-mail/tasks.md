# Tasks

## 1. Authoring

- [x] 1.1 ProtonMail skill: the ten `mail_*` tools, paging by `before_uid`, the folder-scoped UID constraint, the permission prompt on every mutation
- [x] 1.2 ProtonMailBoundary skill: hand off a mailbox request, warn and ask on pasted mail

## 2. Verification

- [x] 2.1 `rune validate` passes with the domain present
- [ ] 2.2 One recorded proof per scenario under `docs/proofs/operate-proton-mail/`
- [ ] 2.3 Bench case for each skill with no skill as baseline (BenchArtifact)

## 3. Record

- [ ] 3.1 Archive moves `adr.md` to `docs/decisions/` with the next free DECK number
