## 1. Content

- [x] 1.1 Replace canonical injection with bounded portable discovery and evidence states
- [x] 1.2 Extract the shared guard policy without changing non-discovery text
- [x] 1.3 Add a complete Claude entrypoint with explicit replace mode and exact probe scopes
- [x] 1.4 Refresh only the existing source subject digest with the established checker

## 2. Verification

- [x] 2.1 Run the content and isolated probe tests
- [x] 2.2 Run candidate Rune assembly tests for complete Claude and Codex bundles
- [x] 2.3 Require the generic and harness source-layer lint to pass without suppressed findings
- [x] 2.4 Verify missing companions and append variants fail acceptance
- [x] 2.5 Check canonical entrypoint schema, prose, source provenance, and the OpenSpec change
- [x] 2.6 Check the source-only alias and the resolved variant links after assembly
- [ ] 2.7 Verify a later native Claude invocation and Codex discovery separately from static checks
- [x] 2.8 Reject printed success markers, fake JSON, padded output, and surviving pipe holders with actual process fixtures

Native invocation is separate acceptance evidence. This change runs no model or deployment command.
The source-only alias keeps source lint active without copying or deploying the guard policy twice.
Assembly checks verify that each provider receives one unchanged regular companion.
