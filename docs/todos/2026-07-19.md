# Backlog: 2026-07-19

- [ ] Adopt `rules/InstallInstructions.md` from forge-core into `runes/core/rules/` with `rune adopt --kind rule`. `SkillInstallation.md` already uses per-skill `INSTALL.md` files. Keep the governing rule beside it. #adoption
- [ ] Add the `kebab-case-skills` assembly transform. Lowercase the directory and frontmatter name for OpenCode, Copilot, and Claude.ai targets. Keep the `SKILL.md` filename. Record the PascalCase-to-kebab mapping in the manifest. The agent transform exists. This work follows the PascalCase source decision. #rune
- [ ] Add the equivalent Codex chat-product packaging adapter at `scripts/harness/codex.py` in BuildSkill. Claude.ai packaging exists. `scripts/harness/__init__.py` defines the adapter interface. #build-skill
- [ ] Add draft provenance to rune. Include the `drafts` review-predicate list, `rune adopt draft record`, and `draftingStatus`. This ceremony stored evidence manually under `.provenance/drafts/`. Implement the council review pattern from Grok finding 12 and the Codex smallest-honest-design. #rune
- [ ] Add the `spec-strict-frontmatter` assembly transform. Move `version`, `upstream`, `when_to_use`, and Claude Code extensions under `metadata:` for `skills-ref` targets. The agentskills specification permits only name, description, license, compatibility, metadata, and allowed-tools at the top level. `skills-ref validate` rejects unknown fields. Combine this transform with `kebab-case-skills`. #rune
