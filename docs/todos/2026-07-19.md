# Backlog — 2026-07-19

- [ ] Adopt the InstallInstructions rule from forge-core (`rules/InstallInstructions.md`) into `runes/core/rules/` via `rune adopt --kind rule` — SkillInstallation.md adopts the per-skill INSTALL.md convention and the governing rule should land beside it #adoption
- [ ] Skills kebab transform at assembly (`kebab-case-skills`: lowercase directory + frontmatter `name:` for opencode/Copilot targets and claude.ai packaging, SKILL.md filename untouched, Pascal-to-kebab mapping recorded in the manifest) — the agents transform exists; skills tracked here after the PascalCase source decision #rune
- [ ] Codex-equivalent chat-product packaging adapter (`scripts/harness/codex.py` in build-skill) — claude.ai packaging shipped first; the adapter contract in `scripts/harness/__init__.py` documents the interface #build-skill
- [ ] Drafts-provenance machinery in rune (`drafts` list in the review predicate, host-side `rune adopt draft record`, `draftingStatus`) — this ceremony sealed evidence manually under `.provenance/drafts/`; promote the pattern per the council review (grok finding 12, codex smallest-honest-design) #rune
- [ ] Metadata-relocation transform at assembly (`spec-strict-frontmatter`: move non-spec frontmatter such as `version`, `upstream`, `when_to_use`, and the Claude Code extensions under `metadata:` for targets validated by `skills-ref`) — the agentskills spec allows only name, description, license, compatibility, metadata, and allowed-tools at top level, and `skills-ref validate` rejects unknown fields; pairs with the kebab transform above #rune
